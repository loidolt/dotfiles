# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/
*.code-workspace

# Nix build artifacts
result
result-*
.direnv/
.envrc

# Hardware configurations - tracked per-host for reproducibility
# Each host's hardware-configuration.nix is generated with:
#   sudo nixos-generate-config --show-hardware-config > hardware-configuration.nix
# Then committed to the repo
!hosts/*/hardware-configuration.nix

# Neovim plugin lock file (generated at runtime)
configs/neovim/lazy-lock.json

# Home Manager backups
*.backup
*.backup.*
*.bak

# Temporary files
*.tmp
*.log

# Node modules (in configs that have them)
node_modules/
bun.lock
package-lock.json
yarn.lock
pnpm-lock.yaml

# Local secrets (not committed to repo)
secrets.nix
home/programs/ssh-hosts.nix

# Claude/AI tool configs (local only)
.claude/

# Personal notes/scratchpad (if you create these)
notes/
scratchpad/
TODO.md
NOTES.md

NIXOS_HOST.yml

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.Python
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
vendor/

# Terraform/Infrastructure
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!*.tfvars.example

# Kubernetes
kubeconfig
*.kubeconfig

# Environment files (except examples)
.env
.env.*
!.env.example.config
.fzf.zsh
.gitconfig
.ssh
.tmux.conf
.zshrc
logs/
result

# Linux packages for apt (Debian/Ubuntu)
# Used when apt is the package manager

# Shell (use system zsh, not brew)
zsh

# Terminal
kitty

# Build essentials
build-essential
gcc
make

# Python development
python3
python3-pip
python3-venv

# System tools
net-tools
software-properties-common

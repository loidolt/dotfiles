# Linux packages for Homebrew (Linuxbrew)
# Used when brew is the package manager on Linux
#
# Note: zsh should be installed via system package manager (apt/dnf/pacman),
# not brew. This avoids PATH and terminfo issues. See docs/LINUX_MIGRATION.md

# Build tools
gcc
make

# Note: python3 is in common.txt
# pip comes with brew's python3

# AI Tools
codex            # OpenAI's Codex coding agent
gemini-cli       # Google Gemini CLI
mcp-toolbox      # Google MCP Toolbox for Databases (for Looker MCP, etc.)

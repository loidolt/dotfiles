# MCP Server Environment Variables
# 
# IMPORTANT: Add these to your shell profile (~/.zshrc, ~/.bashrc, etc.)
# NOT to a .env file, since Claude Code needs them in the shell environment
#
# Example for ~/.zshrc:
#   export REF_API_KEY="your_actual_key_here"
#   export CONTEXT7_API_KEY="your_actual_key_here"
#
# After adding, restart your terminal and launch Claude Code from terminal

# Ref API Key for documentation search
# Get your key from: https://ref.tools
REF_API_KEY=your_ref_api_key_here

# Context7 API Key (if using)
# CONTEXT7_API_KEY=your_context7_api_key_here

# GitHub Copilot (if using)
# GITHUB_TOKEN=your_github_token_here

# Add other API keys as needed

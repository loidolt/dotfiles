# epa-cloidoltlw

Pop!_OS 22.04 LTS workstation.

## Setup Notes

- Uses Linuxbrew for package management (installed at `/home/linuxbrew/.linuxbrew`)
- ZSH from system/nix

## Host-specific Configuration

- `host.sh` - Shell configuration sourced by `.zshrc`
- `ssh-config` - SSH configuration (copied to `~/.ssh/config.local`)

# lra-chrislw

Host-specific configuration for lra-chrislw workstation.

## System Info

- **OS:** Pop!_OS 22.04 LTS
- **Package Manager:** Linuxbrew

## Setup Notes

Migrated from Nix/home-manager to Stow-based configuration.

## Files

- `host.sh` - Shell configuration sourced by `.zshrc`
- `ssh-config` - SSH configuration (optional, copied to `~/.ssh/config.local`)
